"""
basic Queue
uses a List under the hood
"""
class Queue():
    def __init__(self):
        self.items = []
    
    def enqueue(item):
        items.append(item)
    
    def dequeue():
        # list is not empty
        if items:
            return items.pop(len(self.items) - 1)
